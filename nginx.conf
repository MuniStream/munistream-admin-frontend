server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Enable gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml+rss
        application/atom+xml
        image/svg+xml;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    add_header Content-Security-Policy "default-src 'self' http: https: data: blob: 'unsafe-inline'" always;

    # Tenant theme, generated at container start by load-theme.sh.
    #
    # `^~` so this wins over the regex cache block below, and no SPA fallback:
    # without it a missing theme file fell through to `try_files ... /index.html`
    # and was served as HTML with HTTP 200, so the app got markup where it
    # expected JSON and the real problem never showed up in the access log.
    # A missing theme must be a visible 404.
    location ^~ /themes/ {
        try_files $uri =404;
        expires 5m;
        add_header Cache-Control "public, must-revalidate";
    }

    # Handle React Router (SPA)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Note: API calls should go directly to http://api.{tenant}.local.dev
    # No local proxy needed since the main nginx handles routing

    # Static assets caching - disabled for development
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
    }

    # Health check endpoint
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}